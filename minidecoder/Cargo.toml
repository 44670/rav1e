[package]
name = "minidecoder"
version = "0.1.0"
edition = "2021"

[features]
stats = []

[lib]
name = "minidecoder"
path = "src/lib.rs"

[[bin]]
name = "minidecoder"
path = "src/main.rs"

[dependencies]
image = { version = "0.25.0", default-features = false, features = ["png"] }
